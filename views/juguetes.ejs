<head>
    <link rel="stylesheet" href="/style.css">
</head>
<h1>Juguetes</h1>
<ul>
    <% if (locals.prods) { %>
        <% prods.forEach(function(item){ %>
            <li>Nombre: <%= item.name %>, Stock: <%=item.stock%></li>
        <% }) %>
    <% } %>
</ul>